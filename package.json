{
  "name": "toobz-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "serve": "start-dev.sh",
    "build": "coffee -t -m -o dist src && cp -r src/apidocs dist && cp -r src/public dist"
  },
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "snake-camel": "^1.0.8"
  },
  "devDependencies": {
    "coffeescript": "^2.7.0",
    "nodemon": "^2.0.16"
  }
}
